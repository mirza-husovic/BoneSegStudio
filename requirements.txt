# BoneSeg Studio — pinned dependencies (verified against the working
# model330b3 environment, Python 3.11, Windows).
#
# PyTorch is pinned to the CUDA 12.1 build. Install torch/vision from the
# PyTorch index; the rest from PyPI:
#
#   pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 \
#       --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# On a CPU-only machine, install the plain CPU wheels instead:
#   pip install torch==2.5.1 torchvision==0.20.1
# The application auto-detects the absence of CUDA and runs on CPU.

# --- deep learning ---
torch==2.5.1
torchvision==0.20.1
segmentation-models-pytorch==0.5.0
timm==1.0.27

# --- UI (local web server; frontend is vanilla JS, no build step) ---
fastapi==0.136.3
uvicorn==0.49.0
python-multipart==0.0.32

# --- imaging / arrays ---
numpy==1.26.4
pillow==12.2.0
scikit-image==0.26.0
scipy==1.17.1
# 4.10.0.84 is the last release compatible with numpy<2 (4.11+ requires numpy>=2,
# which conflicts with the pinned numpy==1.26.4 above).
opencv-python==4.10.0.84

# --- geospatial / vector ---
rasterio==1.4.4
shapely==2.1.2

# --- skeletonization ---
sknw==0.15
networkx==3.6.1

# --- DXF (reserved for the planned DXF export feature) ---
ezdxf==1.4.3

# --- catalog plate PDF (optional, like ezdxf: export skipped if missing) ---
matplotlib==3.10.8

# --- GCP point files from Excel (optional: .xlsx parsing errors politely if missing) ---
openpyxl==3.1.5
